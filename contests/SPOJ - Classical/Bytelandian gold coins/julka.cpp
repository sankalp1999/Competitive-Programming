#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int t;
    while(cin >> t)
    {
        string a, b;
        cin >> a >> b;
        vector<
        for (int i = 0; i < a.size(); i++)
        {
            
        }
    }
}