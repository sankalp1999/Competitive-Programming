#include <iostream>
#include <vector>
using namespace std;
int main()
{
    vector<int> vec1 = {4,5} ;
    vector<int> vec2 = {12,14,16,18} ;
    vector<int>::iterator it = find(vec2.begin(), vec2.end(),16);
    copy(vec1.begin(),vec1.end(),i_itr) ;
    auto it = vec1.begin();
    for(it;it!=vec1.end();it++)
    { = find(vec2.begin(), vec2.end(),16);
    insert_iterator< vector<i
        cout << vec1.at(it) << endl ;
    }
    return(0);
}
